# Создаётся контейнер, в который загружается наш код, из контейнера он и запускается
FROM node
# Создаём в Докере папке, где будух храниться файлы
WORKDIR /app 
# Копируем файл package*.json в только что созданную директорию
COPY package*.json ./ 
RUN npm install
# Копируем все файлы
COPY . .
# Устанавливаем порт
EXPOSE 4000
CMD ["node", "./src/index.js"]